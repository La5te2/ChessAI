# 英文技术术语表

本表解释 Gadidae 文档中常见英文术语的技术含义。英文技术写作经常借用生活用语，但借用后的含义由数学对象或程序结构决定。例如，`head` 表示网络的输出子网络，`trunk` 表示多个输出共享的主干，而 `entry` 泛指某个索引位置上的值。

## 1. 数学与数据表示

| 英文术语 | 常见中文 | 本文档语境中的含义 |
| --- | --- | --- |
| scalar | 标量 | 单个数值。例如，$V_\theta(s)$ 是一个标量。 |
| vector | 向量 | 按固定顺序排列的一组标量。例如，$\ell_\theta(s)$ 是包含4672个 logits 的向量。 |
| matrix | 矩阵 | 具有行和列的二维数值数组。 |
| tensor | 张量 | 具有一个或多个维度的数值数组。向量和矩阵分别是一维张量和二维张量。 |
| shape | 形状 | 张量在各维度上的大小。例如，$C\times8\times8$ 表示通道、高度和宽度。 |
| dimension | 维度 | 张量的一条坐标轴，也可指该轴的大小。为避免歧义，描述完整尺寸时通常使用 `shape`。 |
| index | 索引 | 标识某个位置的整数。动作索引 $i$ 用于定位一个 Policy logit。 |
| component | 分量 | 向量在某个索引上的标量值。例如，$\ell_\theta(s,i)$ 是向量 $\ell_\theta(s)$ 的一个分量。 |
| element | 元素 | 数学对象中的单个成员。它可指集合成员，也可指矩阵或张量在某个坐标上的数值。 |
| entry | 条目、元素 | 某个索引位置上的内容，是比 `component` 和 `element` 更宽泛的说法。向量宜使用 `component`，矩阵或张量宜使用 `element`，数据集宜使用 `record`。 |
| row | 行 | 矩阵中的一行，或表格型数据集中具有相同字段结构的一条数据。具体含义取决于上下文。 |
| record | 记录 | 数据集中描述一个训练样本的完整数据单元，例如状态、动作索引和 Value target 的组合。 |
| field | 字段 | 一条记录中的一个具名组成部分。 |
| set | 集合 | 不依赖排列顺序的一组对象。例如，$\mathcal A(x)$ 是状态 $x$ 的合法动作集合。 |
| mapping | 映射 | 按确定规则把定义域中的每个输入关联到值域中的一个输出。 |
| distribution | 分布 | 为一组互斥结果分配非负概率且概率总和为1的函数。 |
| normalization | 归一化 | 按共同尺度变换一组数值。概率归一化使所有结果的权重之和等于1。 |
| expectation | 期望 | 各个可能结果按其概率加权后的平均值。Value 使用胜、和、负结果的期望尺度。 |
| coefficient | 系数 | 乘在另一个量上的标量，用于控制该量在公式中的作用强度。 |
| parameter | 参数 | 由训练或配置确定的量。网络权重是可训练参数，搜索常数是配置参数。 |
| estimate | 估计值 | 根据有限数据、模型或搜索得到的近似值。 |
| exact value | 精确值 | 由规则或完整计算唯一确定的值，例如终局的胜负结果。 |

## 2. 国际象棋与棋类引擎

### 2.1 状态与动作

| 英文术语 | 常见中文 | 本文档语境中的含义 |
| --- | --- | --- |
| state | 状态 | 决定后续合法走法和终局规则所需的信息。完整状态包括棋盘、行棋方、王车易位权、吃过路兵状态、计数器和重复历史。 |
| position | 局面 | 对棋盘情形的较宽泛称呼。严格讨论棋规时使用 `state`，因为相同棋子摆放可能对应不同的完整状态。 |
| encoded state | 编码状态 | 状态编码器从完整状态中提取并转换出的固定数值表示。 |
| network input | 网络输入 | 实际传入网络的编码状态。 |
| action | 动作 | 数学和搜索语境中的一个合法选择。国际象棋中的 action 通常对应一个 move。 |
| move | 走法 | 国际象棋规则定义的具体着法，包含起点、终点以及升变等附加信息。 |
| legal action | 合法动作 | 在当前完整状态下符合全部棋规的动作。 |
| action index | 动作索引 | 动作编码器分配给走法的固定整数编号，用于定位网络输出。 |
| side to move | 行棋方 | 当前轮到执行走法的一方。Value 和搜索回报通常采用行棋方视角。 |
| ply | 半回合 | 一方完成的一步棋。白方一步和黑方一步合计两个 plies。 |
| terminal state | 终局状态 | 已由将杀、和棋规则或其他终止条件结束的状态。 |
| nonterminal state | 非终局状态 | 对局仍可继续且存在后续合法动作的状态。 |

### 2.2 棋盘、棋子与规则

| 英文术语 | 常见中文 | 本文档语境中的含义 |
| --- | --- | --- |
| board | 棋盘 | 由8条纵线和8条横线组成的 $8\times8$ 方格区域。 |
| square | 格子 | 棋盘上的一个位置，由纵线字母和横线数字共同命名，例如 `e4`。 |
| file | 纵线 | 棋盘上从白方一侧观察时竖直排列的一列格子，以 `a` 至 `h` 命名。单独出现的 `file` 不表示计算机文件，也不必然表示开放线。 |
| rank | 横线 | 棋盘上横向排列的一行格子，以 `1` 至 `8` 命名。它不同于日常英语中的等级或排名。 |
| diagonal | 斜线 | 由同色格沿斜向连续组成的线。象沿斜线移动。 |
| open file | 开放线 | 没有任何白兵或黑兵的纵线。车和后通常可以利用开放线活动。 |
| semi-open file | 半开放线 | 对某一方而言没有己方兵、但仍有对方兵的纵线，也称半开放纵线。 |
| open rank | 开放横线 | 没有兵阻挡、因而有利于重子横向活动的横线。该术语不如 `open file` 常用。 |
| piece | 棋子、子 | 广义上包括兵以及王、后、车、象、马。讨论子力时，狭义的 `piece` 有时只指王和兵以外的棋子，具体范围需要由上下文说明。 |
| pawn | 兵 | 只能向前推进、以斜向方式吃子的棋子，也是 `pawn unit` 与 `centipawn` 的评价基准。 |
| minor piece | 轻子 | 马或象。 |
| major piece | 重子 | 车或后。 |
| material | 子力、子力总值 | 棋盘上现存棋子的数量与通常价值所形成的物质性资源。`material advantage` 表示一方拥有更高的子力价值。 |
| side | 一方 | 白方或黑方。它不同于 `side to move`，后者专指当前行棋方。 |
| kingside | 王翼 | 初始局面中王所在一侧，通常指 `e` 至 `h` 纵线附近的区域。 |
| queenside | 后翼 | 初始局面中后所在一侧，通常指 `a` 至 `d` 纵线附近的区域。 |
| check | 将军 | 行棋后使对方王受到合法攻击的状态。 |
| checkmate | 将杀 | 行棋方的王正在被将军，且不存在解除将军的合法走法。对局立即结束。 |
| mate | 将杀 | `checkmate` 的常用简称。在 UCI 的 `score mate 3` 中，数字3表示距离将杀还有三个 moves。负数表示当前行棋方将被强制将杀。 |
| stalemate | 逼和、无子可动和棋 | 行棋方没有合法走法，但其王没有被将军，因此对局判和。 |
| draw | 和棋 | 对局结果为平局。成因包括逼和、三次重复、五十回合规则和子力不足等。 |
| repetition | 重复局面 | 同一完整棋规局面再次出现。判断三次重复时还需考虑行棋方、王车易位权和吃过路兵权等状态。 |
| threefold repetition | 三次重复 | 同一局面达到三次时，符合条件的一方可以申请和棋。 |
| fifty-move rule | 五十回合规则 | 双方连续50个 moves 没有兵走且没有吃子时，符合条件的一方可以申请和棋。程序通常以100个 plies 计数。 |
| dead position | 死局 | 从当前局面出发，任何合法走法序列都不可能形成将杀的局面。规则直接判定对局为和棋。 |
| insufficient material | 子力不足 | 由于剩余棋子不足而形成死局的常见情形，例如双方只剩王。该术语常被引擎和棋规库用作一组可快速识别的材料条件。 |
| castling | 王车易位 | 王与同侧车在满足规则条件时共同完成的特殊走法。 |
| en passant | 吃过路兵 | 一方兵在对方兵刚前进两格后，可于紧接的一步按特殊规则将其吃掉。 |
| promotion | 升变 | 兵抵达最后一横线后转换为后、车、象或马。 |
| underpromotion | 欠升变 | 兵升变为车、象或马，而不是通常价值最高的后。 |
| opening | 开局 | 对局最初阶段及其已形成理论的一组走法。 |
| opening book | 开局库 | 保存预先选定开局走法或开局局面的数据，用于提供起始变化或产生对战起始局面。 |
| book move | 开局库走法 | 直接来自开局库的走法。它与引擎通过当前搜索自行选择的走法相区别。 |

### 2.3 走法表示与棋局格式

| 英文术语 | 常见中文 | 本文档语境中的含义 |
| --- | --- | --- |
| notation | 记谱法、表示法 | 按固定语法表示走法、局面或完整对局的规则。 |
| algebraic notation | 代数记谱法 | 使用格子坐标和棋子字母记录走法的一类记谱法。 |
| Standard Algebraic Notation (SAN) | 标准代数记谱法 | 面向棋谱阅读的走法表示，例如 `Nf3`、`exd5`、`O-O` 和 `Qxf7#`。它会省略可由局面推断的信息。 |
| UCI move notation | UCI 走法表示 | 使用起点格、终点格和可选升变字母表示走法，例如 `e2e4`、`e7e8q`。它是 UCI 命令中常用的机器可读格式。 |
| Forsyth-Edwards Notation (FEN) | FEN 局面表示 | 用一行文本记录棋子摆放、行棋方、王车易位权、吃过路兵目标格和两个计数器。FEN 不记录此前的完整重复局面历史。 |
| Portable Game Notation (PGN) | PGN 棋谱格式 | 以标签和走法文本记录一盘或多盘国际象棋对局的文本格式，可包含评注、变化分支和赛果。 |
| tag | 标签 | PGN 开头方括号中的具名元数据，例如 `[White "Gadus"]` 和 `[Result "1-0"]`。 |
| comment | 评注 | PGN 中通常写在花括号内的附加文本，可包含自然语言说明或引擎分数。 |
| variation | 变化、变例 | 从某个局面分出的备选走法序列。PGN 通常用圆括号记录非主线变化。 |
| main line | 主线 | 棋谱或分析中作为主要过程展示的走法序列。 |
| principal variation (PV) | 主要变化 | 引擎根据当前搜索结果预计双方将采用的最佳走法序列。它会随着搜索继续而变化。 |
| MultiPV | 多主变化 | UCI 引擎同时报告若干条候选主变化的分析模式。 |
| fullmove number | 回合编号 | FEN 中从1开始、在黑方走棋后递增的完整回合计数。 |
| halfmove clock | 半回合计数 | FEN 中自最近一次兵走或吃子以来累计的 ply 数，用于五十回合规则。它不同于对局已经进行的总 ply 数。 |

### 2.4 引擎评价与协议

| 英文术语 | 常见中文 | 本文档语境中的含义 |
| --- | --- | --- |
| evaluation | 局面评价、评价值 | 引擎对局面结果或优势程度的数值估计。其视角和尺度必须由具体引擎或协议说明。 |
| score | 分数、评价分 | 引擎报告的局面评价。`score` 本身不保证采用厘兵尺度，也不自动说明是白方视角还是行棋方视角。 |
| pawn unit | 兵值单位 | 以一个兵的典型价值作为1的评价尺度。`+0.50` pawn units 等于 `+50 cp`。 |
| centipawn (cp) | 厘兵 | 兵值的百分之一。`+100 cp` 表示约一个兵的优势。厘兵是评价尺度，不是胜率，也不是棋盘上的实际子力差。 |
| mate score | 将杀分 | 表示已搜索到强制将杀的特殊分数。UCI 使用 `score mate n`，而不是用极大的 `cp` 数代替。 |
| White perspective | 白方视角 | 正值表示对白方有利，负值表示对黑方有利。许多注释版 PGN 采用这一视角。 |
| side-to-move perspective | 行棋方视角 | 正值表示对当前行棋方有利，负值表示对当前行棋方不利。神经网络 Value 和搜索回传常采用这一视角。 |
| win-draw-loss (WDL) | 胜和负分布 | 引擎为胜、和、负三种赛果分别给出的概率或比例。三个分量的具体尺度由输出协议说明。 |
| win probability | 胜率 | 从给定局面最终获胜的概率估计。它不能直接等同于 `cp`。 |
| expected result | 预期赛果 | 胜、和、负结果按概率加权后的期望。常见尺度包括 $[0,1]$ 的得分期望和 $[-1,1]$ 的行棋方结果期望。 |
| centipawn loss | 厘兵损失 | 某个实际走法相对于引擎首选走法造成的评价下降，通常从走棋一方的视角计算并限制为非负值。 |
| average centipawn loss (ACPL) | 平均厘兵损失 | 一组实际走法之 centipawn loss 的算术平均值。较低的 ACPL 通常表示走法更接近所用分析引擎的首选。 |
| centipawn regret | 厘兵遗憾值 | 本项目分析工具使用的走法误差量，等于 UCI 引擎首选走法分数与实际走法分数之非负差值。其数值依赖所用引擎及其搜索预算。 |
| depth | 搜索深度 | 引擎为当前分析报告的名义搜索深度。不同搜索算法对 `depth` 的定义可能不同。 |
| selective depth (seldepth) | 选择性深度 | 搜索在某些变化中达到的最大深度，通常大于名义 `depth`。 |
| nodes per second (NPS) | 每秒节点数 | 引擎每秒处理的搜索节点数量。它只能在节点定义与运行条件相近时用于速度比较。 |
| Universal Chess Interface (UCI) | 通用国际象棋接口 | 图形界面与国际象棋引擎之间交换命令、选项、分析信息和最佳走法的文本协议。 |
| engine option | 引擎选项 | 引擎通过 UCI `option` 消息声明、由界面通过 `setoption` 设置的运行参数。 |
| bestmove | 最佳走法 | UCI 引擎完成当前搜索后报告的最终走法。 |
| info | 分析信息 | UCI 引擎在搜索期间报告深度、节点数、分数、PV 等中间信息的消息。 |
| Elo rating | Elo 等级分 | 根据对局结果估计相对棋力的等级分体系。Elo 差值描述期望得分关系，而不是引擎对局面的评价。 |
| net wins | 净胜局数 | 胜局数减去负局数，即 $W-L$。和棋不直接改变该差值。 |
| paired openings | 配对开局 | 两个参赛模型从相同起始局面各执一次白方和黑方，以降低先手和具体开局对比较结果的影响。 |

## 3. 神经网络结构与推理

| 英文术语 | 常见中文 | 本文档语境中的含义 |
| --- | --- | --- |
| architecture | 架构 | 网络由哪些计算模块构成，以及这些模块如何连接。 |
| layer | 层 | 对输入张量执行一种参数化或固定变换的计算单元。 |
| trunk | 主干 | 多个输出路径共享的网络部分。Gadus 的 residual trunk 同时为 Policy head 和 Value head 提供 $h_B$。 |
| backbone | 骨干网络 | 与 `trunk` 含义相近，通常强调负责提取共享特征的主体网络。 |
| stem | 起始层、输入干 | 主干最前面的计算部分，负责把原始网络输入转换成第一组隐藏特征。 |
| head | 输出头 | 从共享特征计算某一类输出的子网络。例如，Policy head 产生 logits，Value head 产生 $V_\theta(s)$。 |
| branch | 分支 | 网络从共享部分分离出的计算路径。一个 branch 可以包含一个或多个 layers。 |
| block | 模块、块 | 按固定结构组合并可重复堆叠的一组 layers，例如 residual block 或 attention block。 |
| feature | 特征 | 网络用于表示输入信息的数值。特征通常由训练自动形成。 |
| feature map | 特征图 | 在空间网格上排列的一组特征值。一个 $8\times8$ feature map 与棋盘格对应。 |
| plane | 平面 | 棋盘编码中的一个 $8\times8$ 二值或数值特征图。 |
| channel | 通道 | 一组并列 feature maps 中的索引维度。通道数表示网络同时维护多少类特征。 |
| convolution | 卷积 | 使用共享的小型权重核在空间网格上提取局部特征的运算。 |
| kernel | 卷积核 | 卷积在每个局部区域使用的权重数组。$3\times3$ kernel 覆盖相邻九个位置。 |
| padding | 填充 | 在输入边界外补充数值。one-square zero padding 表示四周补一圈零。 |
| bias | 偏置项 | 在线性层或卷积层的加权和之后加入的可训练常数。BatchNorm 的 bias 参数 $\beta$ 加在归一化并缩放后的激活值上。参数 bias 不同于统计学中的估计偏差。 |
| activation | 激活值、激活函数 | `activation value` 指网络中间数值，`activation function` 指施加于这些数值的非线性函数。 |
| ReLU | 线性整流函数 | 逐元素计算 $\max(0,z)$ 的激活函数。 |
| batch normalization (BatchNorm) | 批归一化 | 训练时按通道使用当前 minibatch 的均值和方差对激活值进行标准化，再使用可训练的 scale $\gamma$ 和 bias $\beta$ 调整结果。推理时使用训练期间累计的 running statistics。 |
| batch statistics | 批次统计量 | BatchNorm 从当前 minibatch 计算的逐通道均值和方差。训练阶段使用它们归一化当前激活值，并据此更新 running statistics。 |
| running statistics | 运行统计量 | BatchNorm 在训练期间累计的逐通道 running mean 和 running variance。它们通过 minibatch 统计量更新，并在推理阶段用于归一化激活值。它们不是由损失梯度训练的网络参数。 |
| scale and bias parameters | 缩放与偏置参数 | BatchNorm 在归一化后逐通道应用的可训练参数 $\gamma$ 和 $\beta$。计算形式为 $y=\gamma\widehat x+\beta$，二者属于网络参数并由优化器更新。 |
| residual connection | 残差连接 | 把模块输入与该输入的学习变换相加，使网络能够在学习新变换的同时传递原有表示。 |
| embedding | 嵌入 | 把离散编号映射为可训练连续向量的表示方法。 |
| token | 词元、标记 | Transformer 中作为一个处理单位的向量。棋盘网络可以用一个 token 表示一个格子或整个局面。 |
| attention | 注意力 | 根据输入内容计算不同 tokens 之间关联强度并聚合信息的机制。 |
| feed-forward network (FFN) | 前馈网络 | 对每个 token 或特征独立应用的多层非线性变换。 |
| multilayer perceptron (MLP) | 多层感知机 | 由多个线性层和非线性激活组成的前馈网络。 |
| logit | 逻辑值、未归一化分数 | softmax 之前的实数分数。logit 可以为任意实数，其本身不是概率。 |
| softmax | Softmax 归一化 | 对一组 logits 取指数并归一化，从而得到概率分布。 |
| Policy | 策略 | 模型对当前状态下各个合法动作分配的概率分布。 |
| Value | 价值 | 模型对当前状态最终结果期望的标量估计，通常采用当前行棋方视角。 |
| action value | 动作价值 | 在状态中选择某个动作后的结果估计，通常记作 $Q(s,a)$。 |
| forward pass | 前向传播 | 从输入开始依次执行网络计算并得到输出的过程。 |
| inference | 推理 | 使用已经确定的网络参数对新输入计算输出。 |
| neural evaluation | 神经网络评价 | 对一个或一批网络输入执行前向传播并取得 Policy、Value 或其他网络输出的过程。 |

## 4. 监督训练与优化

| 英文术语 | 常见中文 | 本文档语境中的含义 |
| --- | --- | --- |
| supervised training | 监督训练 | 使用预先给定的输入和目标来调整网络参数。 |
| sample | 样本 | 一次训练所使用的数据单位。文档需要强调数据结构时通常使用 `record`。 |
| dataset | 数据集 | 用于训练、验证或分析的一组 records。 |
| label | 标签 | 数据中直接提供的类别或数值答案。`target` 更强调其在损失函数中的数学作用。 |
| target | 目标值、训练目标 | 在损失函数中与模型预测比较的固定数值或分布。 |
| prediction | 预测值 | 网络根据当前参数计算出的输出。训练使 prediction 接近相应 target。 |
| error | 误差 | prediction 与 target 之间的差或其他偏离量。 |
| loss | 损失 | 衡量预测与目标之间不一致程度的标量函数。 |
| objective | 优化目标 | 优化器需要最小化或最大化的完整数学表达式，可由多个 loss terms 组成。 |
| loss term | 损失项 | 完整 objective 中负责一种监督关系的组成部分。 |
| gradient | 梯度 | 损失对参数的偏导数组成的向量，表示损失在参数空间中增长最快的方向。 |
| backpropagation | 反向传播 | 使用链式法则从损失计算各层参数梯度的过程。它不同于 MCTS 中的 `backup`。 |
| optimizer | 优化器 | 根据梯度和自身状态更新网络参数的算法，例如 AdamW。 |
| learning rate | 学习率 | 控制一次参数更新步长的系数。 |
| weight decay | 权重衰减 | 在参数更新中使权重向零收缩的正则化机制。 |
| batch | 批次 | 一次并行处理的一组样本或状态。推理 batch 与训练 minibatch 的用途不同。 |
| minibatch | 小批次 | 一次计算训练损失和梯度的样本子集。 |
| epoch | 轮次 | 训练过程完整遍历一次数据集。若步数上限提前终止遍历，该次遍历不是完整 epoch。 |
| optimizer step | 优化器步 | 优化器根据一次梯度计算更新参数的过程。它不同于棋局中的 move 或 ply。 |
| moment | 矩估计 | AdamW 对历史梯度及其平方维护的移动平均统计量。 |
| gradient clipping | 梯度裁剪 | 当梯度范数过大时按比例缩小梯度，以限制单次更新幅度。 |
| checkpoint | 检查点 | 保存网络参数和架构描述的文件，可供推理或后续算法加载。 |

## 5. 搜索与缓存

| 英文术语 | 常见中文 | 本文档语境中的含义 |
| --- | --- | --- |
| search tree | 搜索树 | 以状态为节点、以动作转移为边组织候选变化的结构。 |
| node | 节点 | 搜索树中的一个状态及其统计信息。 |
| edge | 边 | 从父节点执行一个动作到达子节点的连接及其统计信息。 |
| root | 根节点 | 一次搜索开始时对应当前局面的节点。 |
| leaf | 叶节点 | 当前搜索路径抵达且需要终局判断、网络评价或展开的节点。 |
| expansion | 展开 | 为节点建立合法动作边，并为后续搜索准备子状态。 |
| simulation | 模拟 | MCTS 从根节点选择路径、评价叶节点并回传结果的一次完整过程。 |
| rollout | 推演 | 从某个状态继续生成一段动作序列的泛称。它不一定采用 MCTS 的完整 selection-evaluation-backup 流程。 |
| prior | 先验概率 | 节点展开时由网络 Policy 提供并存入边的初始动作概率。 |
| visit | 访问 | 一次已经完成并回传结果的模拟经过某个节点或边。 |
| visit count | 访问次数 | 已完成模拟经过节点或边的累计次数，通常记作 $N$。 |
| backup | 回传 | 把叶节点结果沿搜索路径逐层传回父节点并更新统计量。它不同于神经网络训练中的 backpropagation。 |
| virtual visit | 虚拟访问 | 批量 MCTS 在等待叶节点评价期间暂时预留路径的计数。 |
| virtual loss | 虚拟损失 | 为并行选择暂时降低已预留路径吸引力的惩罚项。 |
| exploration | 探索 | 把搜索预算分配给访问较少但可能有价值的动作。 |
| exploitation | 利用 | 把搜索预算分配给当前评价较高的动作。 |
| PUCT | PUCT 选择规则 | 结合动作价值、Policy prior 和访问次数选择搜索边的公式。 |
| First Play Urgency (FPU) | 首次访问紧迫度 | 为尚未完成实际访问的边提供初始选择评价的方法。 |
| decision layer | 决策层 | 在模型推理或 MCTS 完成后调整最终走法排序的规则层。 |
| cache | 缓存 | 保存可复用计算结果的数据结构。Gadus 和 Melano 的评价缓存保存网络输出。 |
| cache hit | 缓存命中 | 查询键已存在于缓存中，因此可以直接复用保存的结果。 |
| eviction | 淘汰 | 缓存达到容量限制时删除已有条目的过程。 |
| least recently used (LRU) | 最近最少使用 | 优先淘汰最长时间未被访问之缓存项的策略。 |
| trajectory-aware LRU (TLRU) | 轨迹感知的最近最少使用 | 在 LRU 顺序中额外提升当前搜索轨迹邻域之缓存项优先级的策略。 |

## 6. 易混淆术语

| 术语组合 | 区别 |
| --- | --- |
| `component` / `element` / `entry` | `component` 适合向量分量，`element` 适合集合成员或矩阵、张量元素，`entry` 是泛称。能够确定对象类型时宜使用前两者。 |
| `record` / `field` | `record` 是一条完整数据，`field` 是该数据中的一个组成部分。 |
| `state` / `position` | `state` 强调足以执行棋规的完整信息，`position` 可以只表示棋盘局面。 |
| `action` / `move` | `action` 是数学与搜索中的选择，`move` 是国际象棋中的具体着法。 |
| `Policy` / `logit` | Policy 是概率分布，logit 是产生该分布之前的未归一化分数。 |
| `Value` / `action value` | Value 评价当前状态，action value 评价在当前状态选择某个动作。 |
| `loss` / `objective` | loss 描述一种误差，objective 是优化器处理的完整目标。只有一个损失项时两者可以具有相同数值。 |
| `gradient` / `update` | gradient 是损失相对于参数的导数，update 是优化器依据梯度改变参数的操作。 |
| `backpropagation` / `backup` | backpropagation 计算神经网络参数梯度，backup 更新搜索树统计量。 |
| `batch` / `minibatch` | batch 泛指一次并行处理的数据组，minibatch 特指一次训练更新使用的样本组。 |
| `epoch` / `step` | epoch 是对数据集的一次完整遍历，optimizer step 是一次参数更新。 |
| `trunk` / `head` | trunk 提取多个输出共享的表示，head 从该表示计算一种特定输出。 |
| `plane` / `channel` | plane 是棋盘编码中的一个空间特征图，channel 是网络张量中并列特征图所在的维度。 |
