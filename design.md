# Eleginus

## 1. 设计问题

Eleginus 以基元及其组成的 622 条人类国际象棋公式为知识底座。设计回答两个问题：

1. 给定全部公式参数及其随局面变化的规律，求值器如何以最低成本得到局面评价；
2. 训练过程如何从自我对弈结果中学习这些参数及其变化规律。

第一个问题决定公式表示、单局面内共享、相邻局面共享和参数布局。第二个问题决定灰盒的数学形式、优化目标和训练循环。两部分共享同一个评价函数。

## 2. 公式信号

### 2.1 局面与观察方

设 $s$ 表示国际象棋状态，$o$ 表示评价的观察方，$\bar o$ 表示另一方。静态评价所需的状态包含棋子摆放、行棋方、王车易位权和吃过路兵目标格。

持续评价在整棵搜索树中固定采用白方作为观察方。定义

$$
\tau(s)=
\begin{cases}
+1,&\text{轮到白方行棋},\\
-1,&\text{轮到黑方行棋}.
\end{cases}
$$

搜索使用的当前行棋方评价为

$$
H_{\mathrm{stm}}(s)=\tau(s)H(s,\mathrm W).
$$

观察方在相邻局面之间保持固定，因此走子只触发实际棋盘事实对应的公式变化。当前行棋方视角的转换只作用于最终标量。

搜索状态负责重复判定和五十步规则计数，并在调用静态评价之前完成终局判定。静态评价因而专注于棋盘结构及其对应的局面效用。

### 2.2 基元与公式

公式语言采用有限棋盘上的定型位板代数。基元由原子项和原子操作组成。原子项提供棋盘输入，原子操作把输入映射为类型确定的输出。基元的边界由执行语义确定：每个原子操作对应一段固定且有界的整数或位板指令序列，并向公式构建器公开输入类型、输出类型和状态依赖。

动态原子项包括各颜色各棋种的位板、行棋方、王车易位权和吃过路兵目标格。固定原子项包括棋盘常量、纵线掩码、横线掩码、方向掩码和跳跃棋子的几何掩码。

原子操作包括：

- 位与、位或、位异或和位取反；
- 带棋盘边界的固定位移；
- 位计数、最低置位提取和最高置位提取；
- 整数加减、固定缩放、比较和条件选择；
- 由整数或棋盘格索引的固定表读取。

攻击集合、射线可见性、开放线、通路兵和王区属于基元的组合结果。公式构建器记录这些组合的表达式及其依赖关系，并让重复组合引用同一个中间结果。

以“开放纵线上的车”为例，记 $P_o$ 为观察方的兵位板，$R_o$ 为观察方的车位板。双方兵的并集为

$$
P=P_o\mathbin{\mathtt{or}}P_{\bar o}.
$$

定义带棋盘边界的固定位移 $S_k$。向高行方向的纵线填充满足

$$
U_0=P,
$$

$$
U_{i+1}=U_i\mathbin{\mathtt{or}}S_{8\cdot2^i}(U_i),
\qquad i=0,1,2.
$$

向低行方向的纵线填充满足

$$
D_0=P,
$$

$$
D_{i+1}=D_i\mathbin{\mathtt{or}}S_{-8\cdot2^i}(D_i),
\qquad i=0,1,2.
$$

含兵纵线的全部格子为

$$
V(P)=U_3\mathbin{\mathtt{or}}D_3.
$$

观察方的公式激活为

$$
f_{\mathrm{rook\text{-}open}}(s,o)=
\operatorname{popcount}\left(
R_o\mathbin{\mathtt{and}}\mathop{\mathtt{not}}V(P)
\right).
$$

这条人类公式由棋子位板、固定位移、位或、位取反、位与和位计数组成。其他公式可以直接引用 $V(P)$，因此同一个局面只计算一次该表达式。

公式构建器在登记表达式之前执行常量折叠、结合运算展平、交换运算排序、恒等式化简、幂等式化简和固定强度归约。规范化后的操作及其操作数编号共同确定表达式编号。相同表达式因而共享结果、依赖和更新路径。

每个动态原子项携带编译期依赖掩码。对于具有 $m$ 个操作数的原子操作 $o$，其结果依赖满足

$$
D\left(o(a_1,\ldots,a_m)\right)
=\bigcup_{i=1}^{m}D(a_i).
$$

依赖掩码沿表达式传播至公式根，从而机械确定每条公式受哪些状态变化影响。

### 2.3 评分信号与局面条件

第 $j$ 条人类公式分别产生观察方和另一方的激活值：

$$
F_j(s,o)=
\left(
f_j(s,o),
f_j(s,\bar o)
\right).
$$

公式构建器通过静态区间传播得到 $f_j$ 的整数范围 $[l_j,u_j]$，并定义

$$
m_j=2^{\left\lceil\log_2\max\left(|l_j|,|u_j|\right)\right\rceil}.
$$

每条有效公式满足 $m_j>0$。记 $q_j=2m_j$。尺度 $q_j$ 用于统一不同公式在参数学习中的量纲。

求值器由双方激活直接生成整数评分信号

$$
d_j(s,o)=
f_j(s,o)-f_j(s,\bar o)
$$

和整数局面条件

$$
t_j(s)=
f_j(s,o)+f_j(s,\bar o).
$$

交换观察方后，两个量分别满足

$$
d_j(s,\bar o)=-d_j(s,o),
$$

$$
t_j(s,\bar o)=t_j(s).
$$

这项变换具有逆变换：

$$
f_j(s,o)=\frac{t_j(s)+d_j(s,o)}{2},
$$

$$
f_j(s,\bar o)=\frac{t_j(s)-d_j(s,o)}{2}.
$$

全部公式组成整数评分向量：

$$
d(s,o)=
\begin{bmatrix}
d_1(s,o)&\cdots&d_N(s,o)
\end{bmatrix}^{\mathsf T},
$$

其中 $N=622$。这 $N$ 个 $t_j$ 构成候选条件信号。公式构建器在建立灰盒坐标之前执行精确条件规约：恒定条件的作用并入基础权重，规范化表达式相同的条件共用一个坐标，能够由固定非零整数倍相互转换的条件保留一个本原代表。规约后的条件向量记为

$$
t(s)=
\begin{bmatrix}
t_1(s)&\cdots&t_K(s)
\end{bmatrix}^{\mathsf T},
\qquad K\le N.
$$

每个保留条件的静态范围以同样的方法给出尺度 $r_k>0$。规约过程同时记录候选条件到保留坐标的固定倍率，使公式定义及其条件语义保持完整。被规约的列在全部合法局面上恒定或线性相同，规约前后的灰盒因而具有相同的评价函数集合。

同一条人类知识同时提供有方向的评分信号和观察方对称的候选局面条件。静态区间传播为全部 $d_j$ 和保留的 $t_k$ 选择能够覆盖其完整取值范围的最窄公共 SIMD 整数宽度。统一宽度使公式坐标形成连续数组，并使灰盒读取采用单一类型路径；公式内部的位板和中间整数按各自运算类型执行。

设公式底座中第 $j$ 条公式的贡献为

$$
a_j\left(f_j(s,o)-f_j(s,\bar o)\right),
$$

则灰盒基础权重采用初值

$$
b_j^{(0)}=a_j.
$$

该初值在条件关系矩阵为零时精确复现公式底座的评价。

## 3. 灰盒评价器

### 3.1 工作过程

基础权重向量记为 $b\in\mathbb R^N$，条件关系矩阵记为 $B\in\mathbb R^{N\times K}$。局面 $s$ 中第 $j$ 条公式的有效权重为

$$
u_j(s)=b_j+\sum_{k=1}^{K}B_{jk}t_k(s).
$$

最终评价为

$$
H(s,o)=
d(s,o)^{\mathsf T}u(s)
=
b^{\mathsf T}d(s,o)
+d(s,o)^{\mathsf T}Bt(s).
$$

一次灰盒评价依次完成：

1. 公式程序生成整数评分向量 $d(s,o)$ 和整数条件向量 $t(s)$；
2. 基础参数 $b_j$ 给出第 $j$ 条公式的静态权重；
3. 条件矩阵第 $j$ 行根据整个条件向量计算权重修正；
4. 评分信号 $d_j$ 与修正后的权重 $u_j$ 相乘，全部公式贡献相加得到 $H$。

矩阵坐标 $B_{jk}$ 表示第 $k$ 个非冗余局面条件对第 $j$ 条评分公式权重的影响。完整矩阵给出全部评分公式与非冗余条件之间的调制方向，坐标稀疏约束从这些方向中选择具有稳定收益的关系。基础权重保留每条公式的独立静态贡献，条件矩阵表达公式权重随整个局面的连续变化。

模型以单条人类公式作为评分语义和参数关系的基本单位。源码中的公式分类服务于阅读和维护；评价与训练直接使用公式坐标、基础权重和条件矩阵。固定且可读的公式构成白盒部分，训练得到的稀疏条件关系构成灰盒部分。

### 3.2 初值与对称性

参数初值为

$$
b=b^{(0)},
\qquad
B=0.
$$

因此初始评价等于公式底座，训练随后从零开始学习条件关系。观察方变换保持 $t(s)$ 并令 $d(s,o)$ 变号，所以

$$
H(s,\bar o)=-H(s,o).
$$

该反对称性由变量定义和评价公式共同保证。

## 4. 单局面评价

### 4.1 单局面内共享

每条人类公式最终产生激活值的表达式称为公式根。公式构建器为规范化后的原子项、派生表达式和公式根分配唯一编号，并按依赖关系生成固定的拓扑执行顺序。

一个局面的公式执行从该局面的动态原子项开始。求值器依次计算派生表达式和公式根，由公式根生成 $d_j(s,o)$ 与候选条件，并通过固定条件映射形成 $t_k(s)$。攻击图、射线可见性、兵结构和其他派生表达式可以同时服务于多条公式；求值器计算一次这样的共享结果，全部引用随后读取同一个值。其余单后继表达式融合为定型直线段。

公式执行采用双方成对的 SIMD 表示。黑方棋盘首先作上下镜像，使两个颜色通道具有相同的前进方向；白方位板与规范化后的黑方位板进入不同向量通道。位运算、固定位移、比较、整数加减以及公式根的和差变换以向量宽度同时执行。位计数和位扫描通过目标指令集对应的向量实现或分通道实现，并保持相同的公式接口。

构建器在每个拓扑层内按操作类型和数据宽度排列可并行表达式。每个批次直接绑定对应的定型操作内核，执行计划以类型化输入和输出偏移连接这些批次。完整向量批次执行向量路径，末尾不足一个向量宽度的表达式使用掩码或标量尾部。公式信号、有效权重和灰盒参数分别保存在对齐的连续数组中；整数信号在进入浮点评价时按向量批量扩展。SIMD 是公式程序、灰盒评价、相邻局面更新和 CPU 参数学习的固定实现约束。

设公式执行所需的表达式集合为 $\mathcal E$，表达式 $e$ 的执行成本为 $\kappa_e$。单局面公式执行的成本为

$$
C_{\mathrm{one}}=\sum_{e\in\mathcal E}\kappa_e.
$$

单局面内共享使每个规范化表达式在一次评价中执行一次。

### 4.2 稀疏条件关系

训练以单个矩阵坐标为稀疏单位。对于评分行 $j$，定义活动条件集合

$$
\mathcal K_j=
\left\{
k\mid B_{jk}\ne0
\right\}.
$$

单局面评价首先初始化

$$
u\leftarrow b,
$$

随后遍历全部活动坐标并执行

$$
u_j\leftarrow u_j+B_{jk}t_k.
$$

最终评价由 SIMD 点积得到：

$$
H(s,o)=d(s,o)^{\mathsf T}u(s).
$$

checkpoint 保存基础权重 $b$ 以及全部非零三元组

$$
(j,k,B_{jk}).
$$

checkpoint 为每个非零坐标保存一份参数值。模型装载过程据此生成两个只读执行视图：评分行视图服务于单局面的 SIMD 累加，条件列视图服务于相邻局面的差量更新。两个视图分别连续保存执行所需的索引和系数副本，使热循环顺序读取系数；二者引用同一参数坐标，并在模型更新后由该坐标同步重建。评分索引与条件索引分别采用能够覆盖 $N$ 和 $K$ 个坐标的最窄无符号整数类型，行列边界采用连续偏移数组表示。

设稀疏矩阵含有 $M_B$ 个非零坐标。单局面的灰盒部分执行 $M_B+N$ 次乘加。checkpoint 的参数载荷由 $N+M_B$ 个浮点权重及 $M_B$ 个坐标组成；两个运行时执行视图各自保存 $M_B$ 个连续系数和索引，从而以少量派生内存换取无间接系数读取的 SIMD 热循环。

## 5. 相邻局面共享

### 5.1 共享评价状态

设着法 $m$ 把父局面 $s$ 变为子局面

$$
s'=T(s,m).
$$

观察方 $o=\mathrm W$ 在两个局面中保持固定。

每个搜索线程维护一份持续评价状态：

$$
\mathcal S_E(s)=
\left(
\{z_e(s)\}_{e\in\mathcal E_{\mathrm{keep}}},
d(s,o),
t(s),
u(s),
H(s,o)
\right).
$$

$\mathcal E_{\mathrm{keep}}$ 包含多后继共享表达式和能够截断高代价传播的中间结果。公式根的双方激活可以由 $d$、规约后的 $t$ 及固定条件映射还原，因此直接保存在公式信号中。$z_e(s)$ 表示其他保留表达式 $e$ 在父局面中的最近结果，$u(s)$ 表示灰盒的最近有效权重。走子过程保留全部状态，并用子局面中实际发生变化的值替换对应部分。子局面因而直接复用父局面的其余表达式结果、公式信号和灰盒权重。

这五类量构成求值器的全部可复用局面状态。拓扑顺序、敏感格掩码、表达式使用者和稀疏坐标顺序属于固定公式程序；撤销日志属于当前搜索路径。搜索层可以另外配置置换表或整局面哈希缓存，并通过局面评价接口使用灰盒结果。

公式构建器为相邻局面共享生成以下静态信息：

- 每个表达式的直接使用者；
- 每个表达式的拓扑层级；
- 位板输入对输出的敏感格掩码；
- 占据格变化对应的射线使用者；
- 每个公式根对应的评分坐标及其条件映射；
- 每个条件列对应的活动坐标及评分行。

位板、整数和布尔表达式结果分别保存在类型连续数组中。直接使用者表、拓扑层边界和条件列边界采用连续偏移数组，表达式编号采用能够覆盖公式程序的最窄无符号整数类型。各层事件队列和传播标记在搜索线程建立时一次分配，并在后续着法中复用。

### 5.2 差量传播与评价更新

每步着法首先生成动态原子项的差量。位板差量采用异或，整数差量采用减法，布尔差量采用异或。变化格与敏感格掩码相交后，求值器把受到影响的直接使用者加入对应拓扑层的事件队列。

求值器按拓扑层递增处理事件，使一个表达式的全部前驱先完成更新。同一表达式由多个输入触发时，本轮传播标记使它在所属层执行一次。表达式根据更新后的全部输入计算新值；新值发生变化时，求值器记录旧值并继续触发其使用者。相同结果使传播在该表达式处结束。

射线表达式使用起点格、终点格和占据变化格确定受到影响的滑行子与后继表达式。普通马步主要触发马位板、相关攻击集合和穿过起终点的射线；兵步进一步触发兵攻击、兵形、纵线状态和新开放的滑行射线。

公式根变化后，求值器生成 $\Delta d_j$，并通过固定条件映射合并得到各保留坐标的 $\Delta t_k$。全部评分差量首先更新评价：

$$
H\leftarrow H+\sum_{j\in\Delta\mathcal D_m}\Delta d_j\,u_j,
$$

随后执行 $d_j\leftarrow d_j+\Delta d_j$。此时 $H=d'^{\mathsf T}u$。对于每个变化条件 $t_k$，求值器沿条件列视图访问活动坐标。记

$$
\Delta u_j=B_{jk}\Delta t_k,
$$

则同一次访问执行

$$
u_j\leftarrow u_j+\Delta u_j,
\qquad
H\leftarrow H+d'_j\Delta u_j.
$$

一个评分行由多个条件变化触发时，每次更新都使用已经更新后的 $d'_j$，并把对应的 $\Delta u_j$ 线性累加到 $u_j$ 和 $H$。全部条件差量应用后执行 $t_k\leftarrow t_k+\Delta t_k$。该次序在一次条件列遍历中精确包含评分信号与条件信号同时变化产生的交叉项。

评分差量按 SIMD 宽度批量读取 $\Delta d$ 与 $u$。条件列更新按 SIMD 宽度顺序读取系数，并聚集对应的 $d'$ 和 $u$；目标指令集提供分散指令时直接写回 $u$，其他指令集按通道写回。水平归约把各通道的 $d'_j\Delta u_j$ 加入 $H$，掩码或标量路径负责尾部。

设着法 $m$ 实际触发的表达式集合为 $\mathcal E_m$，变化评分集合为 $\Delta\mathcal D_m$，变化条件集合为 $\Delta\mathcal T_m$，条件列 $k$ 的活动坐标数量为 $\deg(k)$。本步评价成本为

$$
C_{\Delta}(m)=
\sum_{e\in\mathcal E_m}\kappa_e
+
|\Delta\mathcal D_m|
+
\sum_{k\in\Delta\mathcal T_m}\deg(k).
$$

该成本只包含实际执行的表达式、变化评分和变化条件所访问的活动关系。

### 5.3 回退

搜索栈为每步着法保存更新前的 $H$。每个发生变化的保留表达式结果、评分信号、条件信号和有效权重在本步第一次变化时记录一次旧值。传播标记采用递增轮次编号，因此每步更新可以直接复用标记数组。

回退过程按相反顺序恢复本步记录的旧值，随后恢复该步保存的 $H$。父局面的持续评价状态由此精确恢复，并继续服务于搜索树中的下一个子分支。

## 6. 参数学习

### 6.1 固定样本目标

设固定样本集包含 $S$ 个样本。第 $n$ 个样本包含评分向量 $d_n$、条件向量 $t_n$、目标得分 $y_n\in[0,1]$ 和样本权重 $\omega_n>0$。模型给出的预期得分为

$$
p_n=\sigma\left(d_n^{\mathsf T}b+d_n^{\mathsf T}Bt_n\right).
$$

参数目标为

$$
\begin{aligned}
L(b,B)={}&
-\frac{1}{\sum_n\omega_n}
\sum_{n=1}^{S}\omega_n
\left[
y_n\log p_n+(1-y_n)\log(1-p_n)
\right]\\
&+\frac{\lambda_b}{2}
\sum_{j=1}^{N}q_j^2(b_j-a_j)^2\\
&+\frac{\lambda_B}{2}
\sum_{j=1}^{N}\sum_{k=1}^{K}
\left(q_jr_kB_{jk}\right)^2\\
&+\lambda_1
\sum_{j=1}^{N}\sum_{k=1}^{K}
q_jr_k|B_{jk}|.
\end{aligned}
$$

尺度因子 $q_j$ 和 $r_k$ 使该目标等价于对归一化评分与条件坐标使用统一的二次正则和一范数。整数信号直接进入预测与梯度计算，运行和训练采用相同的原始信号表示。

加权交叉熵、二次正则项和一范数均为凸函数。$\lambda_b>0$ 与 $\lambda_B>0$ 使参数目标具有唯一最优解。以 $a_j$ 为中心的正则项保存公式底座提供的有效初值，同时允许自我对弈结果修正每条公式的基础权重。

### 6.2 坐标主动集

固定数据上的预测残差记为

$$
\delta_n=p_n-y_n.
$$

全部条件坐标的数据梯度为

$$
G=
\frac{1}{\sum_n\omega_n}
\mathsf D^{\mathsf T}
\operatorname{diag}(\omega\odot\delta)
\mathsf T,
$$

其中 $\mathsf D$ 和 $\mathsf T$ 分别以样本为行保存整数评分向量与整数条件向量。训练过程按批次累加该矩阵统计量，从而使样本存储规模与批次大小一致。

对于主动集之外的坐标 $(j,k)$，KKT 违反量为

$$
\rho_{jk}=|G_{jk}|-\lambda_1q_jr_k.
$$

主动集训练循环执行以下步骤：

1. 在活动坐标上使用近端优化求解 $b$ 和 $B$；
2. 计算全部候选坐标的梯度统计量；
3. 按 $\rho_{jk}$ 选择违反量最大的坐标；
4. 将这些坐标加入主动集并重新优化全部活动参数；
5. 将近端更新收缩到零的坐标移出主动集；
6. 在全部候选坐标满足 KKT 条件时完成本轮拟合。

本轮完整对局按整盘划分为拟合集和固定验证集。正则路径产生若干稀疏程度不同的候选解，训练过程在固定验证集上选择满足非零坐标预算 $M_{\max}$ 的最优解。$M_{\max}$ 直接限定 checkpoint 体积、单局面评价成本和相邻局面的条件列更新范围。

### 6.3 梯度

单个样本的评价梯度为

$$
\frac{\partial H}{\partial b_j}=d_j,
$$

$$
\frac{\partial H}{\partial B_{jk}}=d_jt_k.
$$

数据项对任意参数 $\theta$ 的梯度为

$$
\frac{\partial L_{\mathrm{data}}}{\partial\theta}
=
\frac{1}{\sum_n\omega_n}
\sum_n
\omega_n(p_n-y_n)
\frac{\partial H_n}{\partial\theta}.
$$

CPU 后端以 SIMD 执行批量预测、梯度累加和近端坐标更新，CUDA 后端使用批量矩阵乘法和并行近端更新。两个后端求解同一个凸目标并产生同一组灰盒参数。

## 7. 自我对弈循环

一次自我对弈产生从白方观察的终局得分

$$
y^{\mathrm W}\in\left\{0,\frac12,1\right\}.
$$

第 $t$ 个根局面的搜索保存 principal leaf $\ell_t$，并计算

$$
\widehat p_t=\sigma\left(H(\ell_t,\mathrm W)\right).
$$

设对局在第 $T$ 个状态终止。TDLeaf 的 $\lambda_{\mathrm{TD}}$ 回报定义为

$$
z_t=
(1-\lambda_{\mathrm{TD}})
\sum_{n=1}^{T-t-1}
\lambda_{\mathrm{TD}}^{n-1}\widehat p_{t+n}
+\lambda_{\mathrm{TD}}^{T-t-1}y^{\mathrm W}.
$$

末端前一个样本的求和为空，并满足 $z_{T-1}=y^{\mathrm W}$。每轮参数求解把右侧的未来评价视为固定目标，因此第 6.1 节的优化仍为凸问题。每盘对局的样本权重之和设为一，使不同长度的完整对局具有相同总权重。

训练轮次执行以下过程：

1. 已接纳 checkpoint 使用带探索的搜索生成完整对局；
2. 搜索保存各根局面的 principal leaf 整数评分向量 $d$ 和整数条件向量 $t$；
3. 完整对局结果形成 TDLeaf 目标；
4. 坐标主动集优化器求解基础权重和条件关系；
5. 候选 checkpoint 使用固定的 1000 个开局进行双方换色对局，共形成 2000 局验收样本；
6. 验收搜索预算高于样本生成预算；
7. 候选相对已接纳 checkpoint 的 Elo 差值具有正的 95% 置信下界时，候选成为下一轮模型。

对局长度上限为 320 ply。达到上限的对局退出本轮训练样本。终局结果、TDLeaf 传播和配对验收共同把参数优化连接到搜索决策质量。

## 8. 代码结构

`atom.hpp` 和 `atom.cpp` 定义不可分解的原子项、原子操作及其公共子表达式规约。`formula.hpp` 声明固定公式集合及其坐标类型；`formula.cpp` 以基元组合定义规范化关系、派生表达式、622 条人类公式及其初始参数。公式声明顺序确定评分坐标，精确条件规约确定非冗余条件坐标。

`execute.cpp` 从公式定义构造固定公式集合，并提供逐基元解释器和与其逐项等价的融合执行器。融合执行器生成稀疏评分与条件坐标，并直接完成静态权重或灰盒权重下的单局面评价。`searcher.cpp` 定义搜索路径上的评价缓存，并实现其压栈、回退和按局面键复用。

model.hpp 和 model.cpp 定义基础参数 $b$、稀疏条件矩阵 $B$、行列索引排列、评价函数、梯度和参数序列化。

train.cpp 负责自我对弈、principal leaf 样本、TDLeaf 目标、坐标主动集优化和配对验收。

checkpoint 的模型载荷保存基础权重向量 $b$ 和非零条件三元组 $(j,k,B_{jk})$。固定公式集合确定基元、公式、参数尺度、整数类型和坐标顺序；checkpoint 表示灰盒的全部可训练状态。
